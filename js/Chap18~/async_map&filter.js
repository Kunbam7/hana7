// const afterTime = sec => new Promise(resolve => setTimeout(resolve, sec * 1000, sec));

// const odds = await Promise.all([1, 2, 3].map
//     (afterTime)).filter(r => {
//         console.log('r: ', r);
//         return r % 2;
// });

// console.log('odds: ', odds);

